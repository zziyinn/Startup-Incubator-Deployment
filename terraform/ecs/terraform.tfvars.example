aws_region = "us-east-2"
project_name = "startup-incubator"
environment = "production"

# ECR repository URLs (replace with actual values)
frontend_image = "REPLACE_WITH_FRONTEND_ECR_URL:latest"
backend_image = "REPLACE_WITH_BACKEND_ECR_URL:latest"

# Resource configurations
frontend_cpu = 256
frontend_memory = 512
backend_cpu = 256
backend_memory = 512
desired_count = 2

# Tags
tags = {
  Environment = "production"
  Project     = "startup-incubator"
  ManagedBy   = "terraform"
} 